# Frontend Source Layout

`src/app` follows the modular structure from architecture documents:
- `core` for runtime cross-cutting concerns
- `shared` for reusable UI/utilities/models
- `layout` for shell/navigation structure
- `features` for domain-aligned feature areas
- `state` for app and feature state concerns

